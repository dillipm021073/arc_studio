# Server Configuration
# Port to run the server on
PORT=5000

# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:port/database_name
DB_NAME=your_database_name

# Session Secret
# Generate a secure random string for production
SESSION_SECRET=your-secret-session-key-change-this-in-production

# Logging Configuration
# Options: 'all' (log all requests), 'error' (only errors), 'none' (no logging)
# Default: 'error'
LOG_LEVEL=error

# Extraction Method
# Options: 'gemini' (Google Gemini AI), 'autox' (AutoX Cloud)
# Default: 'autox'
# Note: AutoX credentials are now stored per-user in Settings
EXTRACTION_METHOD=autox

# Gemini API Configuration (if using Gemini)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key